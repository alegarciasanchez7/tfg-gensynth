# Variables de entorno para producción (JCEF)

# En producción, conectamos con el Core Java real
VITE_USE_MOCK=false

# URL del WebSocket (fallback si JCEF bridge no está disponible)
VITE_WEBSOCKET_URL=ws://localhost:8765
